# Core runtime dependencies used by the training/evaluation scripts.
# Torch wheels are CUDA-specific; install the torch/torchvision pair that
# matches your CUDA runtime first, then install the remaining packages.

numpy==1.26.4
tqdm==4.67.3
Pillow>=11.3.0
safetensors>=0.7.0

# Data preparation
pandas==2.3.3
pyarrow==21.0.0

# InternVL2.5-2B environment used in this experiment
torch==2.1.2
torchvision==0.16.2
transformers==4.37.2
accelerate==0.34.2
timm==0.9.12
einops==0.6.1
sentencepiece==0.1.99
protobuf>=6.33.5

# Qwen2-VL-7B environment used in this experiment
# The Qwen run used torch==2.5.1, torchvision==0.20.1, transformers==5.1.0,
# accelerate==1.12.0, and qwen-vl-utils==0.0.8 in a separate conda env.
qwen-vl-utils==0.0.8
